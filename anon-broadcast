#!/usr/bin/env python3
"""Run the status command from this directory, with no install step."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from anon_broadcast.cli import main

raise SystemExit(main())
